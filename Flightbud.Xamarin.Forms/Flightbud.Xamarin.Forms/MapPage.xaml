<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps" 
             xmlns:models="clr-namespace:Flightbud.Xamarin.Forms.View.Models"
             x:Class="Flightbud.Xamarin.Forms.MapPage">
    <ContentPage.Content>
        <ContentView>
            <models:AviationMap x:Name="AviationMap" 
                                IsShowingUser="True"
                                MapType="Satellite"
                                ItemsSource="{Binding MapItems}"
                                VisibleRegionChanged="VisibleRegionChangedEventHandler">
                <models:AviationMap.ItemTemplate>
                    <DataTemplate>
                        <maps:Pin Position="{Binding Position}" />
                    </DataTemplate>
                </models:AviationMap.ItemTemplate>
            </models:AviationMap>
        </ContentView>
    </ContentPage.Content>
</ContentPage>